<cover-view class="tab-bar">
  <view wx:for="{{list}}" wx:key="index" class="tab-bar-item {{item.isActive ? 'active': ''}}" data-index="{{index}}" bindtap="switchTab">
    {{item.text}}
  </view>

</cover-view>
